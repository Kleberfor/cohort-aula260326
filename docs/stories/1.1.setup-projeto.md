# Story 1.1: Setup do Projeto

## Status
Done

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "build"]

## Story
**As a** desenvolvedor,
**I want** inicializar o projeto Next.js com Tailwind CSS e ShadCN/UI,
**so that** a base técnica esteja pronta para implementar a landing page.

## Acceptance Criteria
1. Projeto Next.js criado com App Router
2. Tailwind CSS configurado e funcional
3. ShadCN/UI instalado com componentes Button, Input e Card disponíveis
4. Variáveis de ambiente configuradas no `.env.local` (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`)
5. Projeto roda localmente sem erros (`npm run dev`)

## 🤖 CodeRabbit Integration
**Primary Type**: Deployment / Setup
**Complexity**: Low

**Quality Gates**:
- [ ] Pre-Commit (@dev): Verificar que config files estão corretos
- [ ] Pre-PR (@devops): Antes de criar PR

**Focus Areas**:
- Secrets: variáveis de ambiente não hardcoded
- Config: tailwind.config e shadcn corretamente configurados

## Tasks / Subtasks
- [x] Criar projeto Next.js com App Router (`npx create-next-app@latest`)
  - [x] Selecionar TypeScript, Tailwind, App Router durante setup
- [x] Instalar e configurar ShadCN/UI (`npx shadcn@latest init`)
  - [x] Adicionar componentes: Button, Input, Card (`npx shadcn@latest add button input card`)
- [x] Criar `.env.local` com as variáveis do Supabase
- [x] Criar `.env.example` com as chaves sem valores
- [x] Verificar que `npm run dev` roda sem erros (AC: 5)

## Dev Notes
- Stack: Next.js 14+ App Router, TypeScript, Tailwind CSS, ShadCN/UI
- Supabase vars: `NEXT_PUBLIC_SUPABASE_URL` e `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- O projeto é uma single page — não precisa de rotas extras além de `/`
- `.env.local` deve estar no `.gitignore` (já está pelo padrão do Next.js)

### Testing
- Sem testes automatizados nesta story — projeto didático
- Validação manual: `npm run dev` e verificar no browser

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-08 | 1.0.0 | Story criada | River |
